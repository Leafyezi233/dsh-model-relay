# Dependencies (the plugin has no runtime dependencies of its own, but a
# developer may install tooling locally).
node_modules/

# npm pack output.
*.tgz

# Editor and OS noise.
.DS_Store
*.swp
*.log

# Local environment overrides.
.env
.env.local
